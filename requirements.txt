# Free-data scaffold — keep deps minimal until ingest needs grow
# mediacloud  # optional, layer D when MC_API_KEY is set
pyarrow>=14.0.0  # LocalView meta parquet + geo crosswalk
pandas>=2.0.0    # LocalView geo crosswalk grouping
# openpyxl       # optional, only if inspecting xlsx ledgers by hand
# pytrends      # optional, layer F Google Trends free pilot (make fetch-google-trends)
